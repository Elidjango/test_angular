import { ShowAndHidePassPipe } from './show-and-hide-pass.pipe';

describe('ShowAndHidePassPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowAndHidePassPipe();
    expect(pipe).toBeTruthy();
  });
});
